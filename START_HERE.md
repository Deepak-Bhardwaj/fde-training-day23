# START HERE

## Objective

Design and build a **Fleet Disruption & Voyage Recovery Intelligence Workbench** that proves the architecture:

**Enterprise Sources → Semantic Foundation → Connected Knowledge → Graph Platform → Hybrid Retrieval → Runtime Context Graph → AI/Agent → Policy/Human Gate → Evidence/Feedback**

Then convert the approved architecture into a PRD and implement the prototype in Google AI Build.

## Sequence

1. Read `Participant_Case_Study_Context_Aware_AI_FDE.md`.
2. Review the two files in `reference_visuals/` and `architecture/Concept_Cheat_Sheet.md`.
3. Run `python scripts/profile_baseline.py`.
4. Inspect all evidence folders. Do **not** remove the deliberate traps.
5. Complete participant Templates 01–12 in order.
6. Complete Template 13 traceability.
7. Generate the PRD using Template 14 + `google_ai_build/01_PRD_GENERATION_PROMPT.md`.
8. Run `python scripts/build_app_fixture.py`.
9. Build in Google AI Build using the master prompt and fixture bundle.
10. Execute all 15 golden scenarios and capture Template 15 evidence.
11. Run `python scripts/sanity_check.py`.

## Non-negotiables

- The LLM is not the architecture.
- No autonomous navigation command/control.
- No critical maintenance-hold bypass.
- No fabricated missing/stale source evidence.
- Duplicate/replayed events cannot create duplicate operational actions.
- Active policy/version and human/technical authority are deterministic controls.
- Vessel essential workflow remains usable without cloud AI.
- External documents/messages are untrusted instructions.
- Decision traces record observable evidence/rationale, not hidden chain-of-thought.
